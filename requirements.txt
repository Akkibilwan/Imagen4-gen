    # requirements.txt
    # For Streamlit App - Updated: May 22, 2025
    # Using more flexible versioning to improve installation success.

    # --- Core Application ---
    streamlit>=1.34.0 # Use a known recent stable base
    Pillow>=10.3.0   # Use a known recent stable base

    # --- Google Cloud AI & Auth (Focus on Gemini API) ---
    # Ensure you have a version that supports the Gemini features you need.
    # Check PyPI for the latest stable google-generativeai version if issues persist.
    google-generativeai>=0.5.0 
    google-auth>=2.29.0
    
